# Install command-line tools using Homebrew
# Usage: `brew bundle Brewfile`

# Make sure we’re using the latest Homebrew
update

# Upgrade any already-installed formulae
upgrade

install ack
install bash # Bash 4
install ctags
install exercism
install git
install heroku-toolbelt
install hub
install mackup
install redis
install ssh-copy-id
install tmux
install vim --override-system-vi # Better/newer Vim
install wget

# Install some prog langs, let RVM handle Rubies
install crystal
install elixir
install go --cross-compile-common
install groovy
install node
install rust
install sbt

# Remove outdated versions from the cellar
cleanup
